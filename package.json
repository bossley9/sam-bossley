{
  "scripts": {
    "build": "DEBUG=* eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf ./_site/"
  },
  "devDependencies": {
    "@11ty/eleventy-plugin-rss": "^1.1.1",
    "clean-css": "^5.1.5",
    "eleventy": "^1.0.6",
    "html-minifier": "^4.0.0"
  }
}
